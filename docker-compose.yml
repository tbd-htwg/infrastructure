services:
  caddy:
    image: ghcr.io/tbd-htwg/caddy-google:latest
    restart: always
    ports:
      - "80:80"
      - "443:443"
    environment:
      - CADDY_DOMAIN=${CADDY_DOMAIN}
      - GCP_PROJECT=${GCP_PROJECT}
      - GOOGLE_APPLICATION_CREDENTIALS=${GOOGLE_APPLICATION_CREDENTIALS}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:Z
      - ./data:/data:rw,Z
      - ./config:/config:rw,Z
      - ./static:/srv/static:ro,Z
      - ./logs:/var/log/caddy:rw,Z
      # Uncomment after placing a service account JSON and setting GOOGLE_APPLICATION_CREDENTIALS in .env:
      # - ./gcp-sa.json:/secrets/gcp-sa.json:ro,Z
    networks:
      - caddy-network

  frontend:
    image: ghcr.io/tbd-htwg/frontend:main
    restart: always
    networks:
      - caddy-network

  backend:
    image: ghcr.io/tbd-htwg/backend:main
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
      - SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver
      - SPRING_JPA_PROPERTIES_HIBERNATE_SEARCH_BACKEND_HOSTS=elasticsearch:9200
    networks:
      - caddy-network

  postgres:
    image: postgres:18
    restart: always
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - caddy-network

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.12.0
    restart: always
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false 
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sSf http://localhost:9200 >/dev/null || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - caddy-network

volumes:
  caddy_data:
  caddy_config:
  postgres_data:
  elasticsearch_data:

networks:
  caddy-network:
    driver: bridge