{{- $rollingUpdate := default dict .Values.services.social.rollingUpdate }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-service
  labels:
    {{- include "tripplanning.labels" . | nindent 4 }}
    app.kubernetes.io/component: social-service
spec:
  replicas: {{ include "tripplanning.socialReplicas" . }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: {{ default 0 $rollingUpdate.maxSurge }}
      maxUnavailable: {{ default 1 $rollingUpdate.maxUnavailable }}
  selector:
    matchLabels:
      app.kubernetes.io/component: social-service
      {{- include "tripplanning.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/social-config: {{ include (print $.Template.BasePath "/configmaps/social-configmap.yaml") . | sha256sum }}
        traffic.sidecar.istio.io/excludeOutboundIPRanges: 169.254.169.254/32
        traffic.sidecar.istio.io/excludeOutboundPorts: "5432,6379,9200"
      labels:
        app.kubernetes.io/component: social-service
        {{- include "tripplanning.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
      {{- range .Values.global.imagePullSecrets }}
        - name: {{ . }}
      {{- end }}
      {{- end }}
      containers:
        - name: social-service
          image: "{{ .Values.services.social.image.repository }}:{{ .Values.services.social.image.tag }}"
          imagePullPolicy: {{ .Values.services.social.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.social.port }}
          envFrom:
            - configMapRef:
                name: social-service-config
{{- range .Values.services.social.secretRefs }}
            - secretRef:
                name: {{ . }}
{{- end }}
          startupProbe:
            httpGet:
              path: {{ .Values.services.social.livenessPath }}
              port: {{ .Values.services.social.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 24
          readinessProbe:
            httpGet:
              path: {{ .Values.services.social.readinessPath }}
              port: {{ .Values.services.social.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: {{ .Values.services.social.livenessPath }}
              port: {{ .Values.services.social.port }}
            initialDelaySeconds: 40
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests:
              cpu: {{ .Values.services.social.resources.requests.cpu }}
              memory: {{ .Values.services.social.resources.requests.memory }}
            limits:
              cpu: {{ .Values.services.social.resources.limits.cpu }}
              memory: {{ .Values.services.social.resources.limits.memory }}
